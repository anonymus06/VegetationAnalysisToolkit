% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_measurement_data.R
\name{plot_measurement_data}
\alias{plot_measurement_data}
\title{Plot Measurement Data}
\usage{
plot_measurement_data(
  output_path,
  variable,
  code = NULL,
  tstart = NULL,
  tend = NULL,
  plot_type = "time_series"
)
}
\arguments{
\item{output_path}{A character string specifying the path to the directory containing the processed data files.}

\item{variable}{The name of the variable to plot (e.g., 'NDVI', 'CCI').}

\item{code}{The specific code to filter by (optional).}

\item{tstart}{The start date for filtering (optional).}

\item{tend}{The end date for filtering (optional).}

\item{plot_type}{The type of plot to generate: "time_series", "boxplot", or "violin" (default is "time_series").}
}
\value{
A plotly object of the specified plot.
}
\description{
This function generates various types of plots for a single measurement variable
(e.g., NDVI, CCI) over time or as a distribution. It allows for filtering by
measurement location codes and date ranges.
}
\details{
The function plots the selected variable for the specified code and date range, or the entire dataset if no
filters are provided.
It allows generating a time series plot, box plot, or violin plot for the data values.
The date range is set using \code{tstart} and \code{tend}.
}
\examples{
\dontrun{
output <- "out/"

plot_measurement_data(output, variable = "NDVI")

plot_measurement_data(output, variable = "CCI", code = "CSTA", tstart = "2023-05-12", tend = "2023-08-18", plot_type = "boxplot")
}
}
